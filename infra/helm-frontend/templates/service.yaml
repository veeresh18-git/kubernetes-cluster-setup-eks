apiVersion: v1
kind: Service
metadata:
  name: {{ include "ds-app.fullname" . }}
  labels:
    {{- include "ds-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ds-app.selectorLabels" . | nindent 4 }}
